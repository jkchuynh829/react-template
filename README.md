### A react template with minimal configuration

#### Uses:

- React/ReactDOM
- Parcel
- ESLint

That's pretty much it!

#### How to use

Install:

```
npm i
```

Run on `localhost:1234`:

```
npm run dev
```
